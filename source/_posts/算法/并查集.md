---
title: 并查集
date: 2022-12-27 07:42:43
tags: 算法
categories: 算法
cover: https://tva4.sinaimg.cn/large/0072Vf1pgy1foxkchavg0j31hc0u04gv.jpg
date: 2022-12-19 11:19:04
---

# 并查集概述

并查集是一种树型的数据结构，用于处理一些不相交集合（Disjoint Sets）的合并及查询问题

## 并查集操作

查：查找两个元素是否在一个集合（或者说查找元素的根节点）

并：合并两个集合

初始并查集(每个元素都为一个集合，一个集合可以挂载到另一个集合上)

```
a b c d e f  初始都指向自身
e->a  b c f  a和e合并,t他们是在同一个集合
e->a  c->a   a和c合并,a的个数大，小挂大
```

# 并查集代码

```java
package class14;

import java.util.HashMap;
import java.util.List;
import java.util.Stack;

public class Code05_UnionFind {

	//封装为集合
	public static class Node<V> {
		V value;

		public Node(V v) {
			value = v;
		}
	}

	public static class UnionFind<V> {
		// 变为并查集
		public HashMap<V, Node<V>> nodes;
		//所有并查集他的父元素(直系，只有一层，父亲和子)
		public HashMap<Node<V>, Node<V>> parents;

		//只有最上层的父才有的大小
		public HashMap<Node<V>, Integer> sizeMap;

		//集合转为并查集
		public UnionFind(List<V> values) {
			nodes = new HashMap<>();
			parents = new HashMap<>();
			sizeMap = new HashMap<>();
			for (V cur : values) {
				Node<V> node = new Node<>(cur);
				nodes.put(cur, node);
				parents.put(node, node);
				sizeMap.put(node, 1);
			}
		}

		// 给你一个节点，请你往上到不能再往上，把代表返回
		public Node<V> findFather(Node<V> cur) {
			//一个栈，用于所有节点赋值给父元素的
			Stack<Node<V>> path = new Stack<>();
			//一直查询父元素
			while (cur != parents.get(cur)) {
				path.push(cur);
				cur = parents.get(cur);
			}

			//所有元素的父元素直接指向最顶级的父
			while (!path.isEmpty()) {
				parents.put(path.pop(), cur);
			}
			return cur;
		}


		//他们的父是否相等
		public boolean isSameSet(V a, V b) {
			return findFather(nodes.get(a)) == findFather(nodes.get(b));
		}

		//联合
		public void union(V a, V b) {
			//最顶级的父
			Node<V> aHead = findFather(nodes.get(a));
			Node<V> bHead = findFather(nodes.get(b));
			if (aHead != bHead) {
				//大小
				int aSetSize = sizeMap.get(aHead);
				int bSetSize = sizeMap.get(bHead);
				//决定谁挂谁  小挂大
				Node<V> big = aSetSize >= bSetSize ? aHead : bHead;
				Node<V> small = big == aHead ? bHead : aHead;
				//小的元素的父重新指向
				parents.put(small, big);
				//大小修改
				sizeMap.put(big, aSetSize + bSetSize);
				//小直接删除
				sizeMap.remove(small);
			}
		}

		public int sets() {
			return sizeMap.size();
		}

	}
}
```

 
