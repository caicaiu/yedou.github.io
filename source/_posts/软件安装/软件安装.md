---
title: 软件安装
tags: 软件安装
categories: 软件安装教程
cover: https://www.helloimg.com/images/2022/02/14/GgCzt5.jpg
abbrlink: f025fac7
date: 2023-01-07 11:20:06
---

# Linux Docker安装

## 查看是否满足条件

1. 确定你是CentOS7及以上版本

	```
	 cat /etc/redhat-release
	```

2. CentOS7能上外网

##  安装步骤

1. 下载gcc相关的东西

	```
	 yum -y install gcc
	 yum -y install gcc-c++
	```

2. 卸载docker旧版本

	```
	 yum -y remove docker docker-common docker-selinux docker-engine
	```

3. 设置镜像仓库

	> 因为如果我们访问的是国外的网站，网速就会变得很慢，有时候还会因为连接的问题而报错，所以我们配置阿里云的镜像网站

	```
	 yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo
	```

4. 更新yum软件包索引

	```
	 yum makecache fast
	```

5. 安装DOCKER

	```
	 yum -y install docker-ce
	```

6. 启动docker

	```
	 systemctl start docker
	```

7. 测试

	> 输出Hello from Docker!

	```
	 docker version #查看docker版本号
	 docker run hello-world #在镜像中下载hell-world这个容器
	```

8. 配置阿里云镜像加速

	1. 登录阿里云开发者平台

	2. 搜索**容器镜像服务**

	3. 镜像工具，镜像加速

	4. 选择centOS，复制到linxu执行

	5. 重启dockekr   service docker restart

	6. 查看是否配置成功   如果是你的镜像就可以了

		```
		cat /etc/docker/daemon.json
		```

9. 卸载docker

	```
	 systemctl stop docker 
	 yum -y remove docker-ce
	 rm -rf /var/lib/docker
	```

10. 在run的时候底层都干了写什么![在这里插入图片描述](https://xingqiu-tuchuang-1256524210.cos.ap-shanghai.myqcloud.com/4869/20210209143442909.png)

# Linux 安装OpenResty

OpenResty 是一个基于 Nginx的高性能 Web 平台，用于方便地搭建能够处理超高并发、扩展性极高的动态 Web 应用、Web 服务和动态网关。

相当于是一个nginx,用于做nginx进群缓存

## 安装步骤

1. 安装开发库

	```
	yum install -y pcre-devel openssl-devel gcc --skip-broken
	```

2. 安装OpenResty仓库

	```
	yum-config-manager --add-repo https://openresty.org/package/centos/openresty.repo
	```

	如果提示说命令不存在，则运行：

	```
	yum install -y yum-utils 
	```

	然后再重复上面的命令

3. 安装OpenResty

	```
	yum install -y openresty
	```

	

4. 安装opm工具

	opm是OpenResty的一个管理工具，可以帮助我们安装一个第三方的Lua模块。

	```
	yum install -y openresty-opm
	```

5. 目录结构

	默认情况下，OpenResty安装的目录是：/usr/local/openresty

6. 配置nginx的环境变量

	1. 打开配置文件：

		```
		vi /etc/profile
		```

	2. 在最下面加入两行：

		```
		export NGINX_HOME=/usr/local/openresty/nginx
		export PATH=${NGINX_HOME}/sbin:$PATH
		```

		

	3. 然后让配置生效：

		```
		source /etc/profile
		```

		



7. 启动和运行

	```
	# 启动nginx
	nginx
	# 重新加载配置
	nginx -s reload
	# 停止
	nginx -s stop
	```

	

8. 配置nginx.conf中的注释部分删除
	修改`/usr/local/openresty/nginx/conf/nginx.conf`文件，内容如下：

	```
	#user  nobody;
	worker_processes  1;
	error_log  logs/error.log;
	
	events {
	    worker_connections  1024;
	}
	
	http {
	    include       mime.types;
	    default_type  application/octet-stream;
	    sendfile        on;
	    keepalive_timeout  65;
	
	    server {
	        listen       8081;
	        server_name  localhost;
	        location / {
	            root   html;
	            index  index.html index.htm;
	        }
	        error_page   500 502 503 504  /50x.html;
	        location = /50x.html {
	            root   html;
	        }
	    }
	}
	```

	

8. 在Linux的控制台输入命令以启动nginx：

	```
	nginx
	```

	

9. 然后访问页面：http://192.168.150.101:8081，注意ip地址替换为你自己的虚拟机IP：



## 备注

加载OpenResty的lua模块：

```nginx
#lua 模块
lua_package_path "/usr/local/openresty/lualib/?.lua;;";
#c模块     
lua_package_cpath "/usr/local/openresty/lualib/?.so;;";  
```

common.lua

```lua
-- 封装函数，发送http请求，并解析响应
local function read_http(path, params)
    local resp = ngx.location.capture(path,{
        method = ngx.HTTP_GET,
        args = params,
    })
    if not resp then
        -- 记录错误信息，返回404
        ngx.log(ngx.ERR, "http not found, path: ", path , ", args: ", args)
        ngx.exit(404)
    end
    return resp.body
end
-- 将方法导出
local _M = {  
    read_http = read_http
}  
return _M
```



释放Redis连接API：

```lua
-- 关闭redis连接的工具方法，其实是放入连接池
local function close_redis(red)
    local pool_max_idle_time = 10000 -- 连接的空闲时间，单位是毫秒
    local pool_size = 100 --连接池大小
    local ok, err = red:set_keepalive(pool_max_idle_time, pool_size)
    if not ok then
        ngx.log(ngx.ERR, "放入redis连接池失败: ", err)
    end
end
```

读取Redis数据的API：

```lua
-- 查询redis的方法 ip和port是redis地址，key是查询的key
local function read_redis(ip, port, key)
    -- 获取一个连接
    local ok, err = red:connect(ip, port)
    if not ok then
        ngx.log(ngx.ERR, "连接redis失败 : ", err)
        return nil
    end
    -- 查询redis
    local resp, err = red:get(key)
    -- 查询失败处理
    if not resp then
        ngx.log(ngx.ERR, "查询Redis失败: ", err, ", key = " , key)
    end
    --得到的数据为空处理
    if resp == ngx.null then
        resp = nil
        ngx.log(ngx.ERR, "查询Redis数据为空, key = ", key)
    end
    close_redis(red)
    return resp
end
```

开启共享词典：

```nginx
# 共享字典，也就是本地缓存，名称叫做：item_cache，大小150m
lua_shared_dict item_cache 150m; 
```

# Docker 安装Mysql

1. 为了方便后期配置MySQL，我们先准备两个目录，用于挂载容器的数据和配置文件目录：

	```
	# 进入/opt目录
	cd /opt
	# 创建文件夹
	mkdir mysql
	# 进入mysql目录
	cd mysql
	```

2. 运行命令
	进入mysql目录后，执行下面的Docker命令：

	```
	docker run \
	 -p 3306:3306 \
	 --name mysql \
	 -v $PWD/conf:/etc/mysql/conf.d \
	 -v $PWD/logs:/logs \
	 -v $PWD/data:/var/lib/mysql \
	 -e MYSQL_ROOT_PASSWORD=123456 \
	 --privileged \
	 -d \
	 mysql:5.7.25
	```

3. 修改配置

	在/opt/mysql/conf目录添加一个my.cnf文件，作为mysql的配置文件：

	```
	# 创建文件
	touch /opt/mysql/conf/my.cnf
	```

	添加内容

	```
	[mysqld]
	skip-name-resolve
	character_set_server=utf8
	datadir=/var/lib/mysql
	server-id=1000
	```

4. 配置修改后，必须重启容器：

	```
	docker restart mysql
	```

5. 查看是否启动成功  看到mysql的stutas是up就可以了

	```
	docker ps -a 
	```

	

# Linux 安装Nginx

1. 安装插件

	```
	yum -y install gcc zlib zlib-devel pcre-devel openssl openssl-devel
	```

2. 下载ngin，拖入opt下载好的 nginx.tar.gz 压缩包

3. 解压

	```
	tar -xvf nginx-1.13.7.tar.gz
	```

4. 在文件夹中执行./configure，然后执行**make && make install**
5. 进入目录 /usr/local/nginx/sbin/ 执行该命令./nignx
6. 然后使用命令 **ps -ef |grep nginx** 查看服务是否启动
7. 在window下输入，查看是否成功访问   http://192.168.xx.xxx:80/
	![image-20220817140306638](https://xingqiu-tuchuang-1256524210.cos.ap-shanghai.myqcloud.com/4869/image-20220817140306638.png)

8. 如果没有启动，那么就关闭防火墙亦或者不拦截Nginx的端口

	```
	systemctl stop firewalld
	#不拦截端口
	firewall-cmd --add-port=80/tcp --permanent
	firewall-cmd --reload
	```

	

# Window 安装Nginx

[windows下安装nginx](https://www.cnblogs.com/qfb620/p/5508468.html#:~:text=到nginx官网上下载相应的安装包， http%3A%2F%2Fnginx.org%2Fen%2Fdownload.html ；,进入window的cmd窗口，输入如下图所示的命令，进入到nginx目录（D%3A%2Fnginx-1.8.1），使用“ start nginx.exe ”进行nginx的安装，如下图所示：)

# Linux 安装jdk 和tomcat

[JDK.target](https://repo.huaweicloud.com/java/jdk/)

[jdk和tomcat](https://cloud.tencent.com/developer/article/1538210)
